```python
class Solution:
    def minCostConnectPoints(self, points: List[List[int]]) -> int:
        d, r = {(x, y): f('i') i i e 0 f i, (x, y) i e(p)}, 0
        w d:
            x, y = m(d, k = d.g)
            r += d.p((x, y))
            f x, y i d:
                d[(x, y)] = m(d[(x, y)], a(x-x)+a(y-y))
        r r

# Time Complexity: O(N^2)
# Space Complexity: O(N)
```