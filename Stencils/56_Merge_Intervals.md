```python
class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        i.s(k=l p: p[0])
        o = [i[0]]

        f s, e i i:
            l = o[-1][1]
            i s <= l:
                o[-1][1] = m(l, e)
            e:
                o.a([s, e])
        r o

# Time Complexity: O(N log N)
# Space Complexity: O(N)
```